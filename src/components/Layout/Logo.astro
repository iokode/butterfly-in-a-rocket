---
/**
 * The IOKode mark and wordmark.
 *
 * The mark is drawn in detached pieces — wings, body and antennae never touch. It is inlined
 * rather than loaded as an image so it can paint in `currentColor`, which the lockup sets once to
 * `--text-strong`: mark and wordmark are always the same colour, and one file serves both themes.
 * The wordmark is Chakra Petch 600, cased exactly "IOKode" — never IOKODE, never iokode.
 *
 * `size` is the mark's height; the wordmark and the gap are derived from it at the design
 * system's ratios (0.95 and 0.34), so the lockup scales as one object.
 */

interface Props {
    /** Mark height in pixels. The mark stops being legible below 20; use the favicon there. */
    size?: number;
    /** `lockup` is the mark plus the wordmark; `mark` is the butterfly alone. */
    variant?: 'lockup' | 'mark';
}

const {size = 32, variant = 'lockup'} = Astro.props;
---

<a class="logo" href="/" aria-label="IOKode, home" style={`gap: ${size * 0.34}px`}>
    <svg
        xmlns="http://www.w3.org/2000/svg"
        viewBox="0 0 52 44"
        width={size * 52 / 44}
        height={size}
        role="img"
        aria-hidden="true"
        focusable="false"
    >
        <g fill="currentColor">
            <path d="M26 9 27.6 13.5 27.2 30 26 34.5 24.8 30 24.4 13.5z"></path>
            <path d="M27.3 8.5 33.4 2.6l1.9 1.7-6.1 5.9zM24.7 8.5 18.6 2.6l-1.9 1.7 6.1 5.9z"></path>
            <path d="M23.2 12 3 1.5 1 17.5l10.5 5.5 11.7-2.5z"></path>
            <path d="M28.8 12 49 1.5l2 16-10.5 5.5-11.7-2.5z"></path>
            <path d="M23.2 23.5 9.5 26.5 14.5 41l8.7-9.5z"></path>
            <path d="M28.8 23.5 42.5 26.5 37.5 41l-8.7-9.5z"></path>
            <circle cx="18.5" cy="2.8" r="2.6"></circle>
            <circle cx="33.5" cy="2.8" r="2.6"></circle>
        </g>
    </svg>

    {variant === 'lockup' && <span class="wordmark" style={`font-size: ${size * 0.95}px`}>IOKode</span>}
</a>

<style>
    .logo {
        /* Box model */
        display: inline-flex;
        align-items: center;

        /* Typographic */
        color: var(--text-strong);
        text-decoration: none;

        /* The lockup is a mark, not a link that reacts — it holds still on hover. */
        &:hover {
            /* Typographic */
            color: var(--text-strong);
            text-decoration: none;
        }

        > svg {
            /* Box model */
            display: block;
            flex: none;
        }

        > .wordmark {
            /* Typographic */
            font-family: var(--font-display);
            font-weight: var(--weight-semibold);
            line-height: 1;
            letter-spacing: var(--tracking-tight);
        }
    }
</style>
