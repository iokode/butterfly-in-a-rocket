---
import { getEntry } from "astro:content";
import Content from "../layouts/Content.astro";

const entry = await getEntry('importants', 'legal');
const LegalNotice = (await entry.render()).Content;
---

<Content title="Legal notice">
    <LegalNotice />
</Content>
